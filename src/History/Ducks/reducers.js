

// export default function historyReducer(state = {trips: state.trips}, action){
//   switch (action.type) {
 
//     case 'DELETE_TRIP':
//       const newTrips = state.trips.filter(el => el.id !== action.tripId)
//       return { ...state, trips: newTrips};
    
//     default:
//       return state;
//   }
// }